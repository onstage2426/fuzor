<?php

declare(strict_types=1);

namespace Fuzor\Exceptions;

/** Thrown when a user-supplied argument is invalid (bad language tag, missing id, duplicate id). */
class QueryException extends FuzorException
{
}
